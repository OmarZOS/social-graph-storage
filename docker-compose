version: '3.5'
services:
  storage-server:
    build: server/
    image: omarzos/storage-server:latest
    container_name: phoros-storage-server
    environment:
      - ELASTICSEARCH_CLUSTER_NAME=elasticsearch-cluster
      - ELASTICSEARCH_CLUSTER_HOSTS=elasticsearch-node1,elasticsearch-node2
      - ELASTICSEARCH_NODE_NAME=elastic-node1
      - NEBULA_HOST=127.0.0.1
      - NEBULA_PORT=9669
      - NEBULA_USER=root
      - NEBULA_PASS=nebula
    networks:
      - phoros_net
networks:
  phoros_net:
    external: true



